import {styled, DialogContent} from "@mui/material";

const ActionDialogContent = styled(DialogContent)(() => ({
    display: "flex",
    flexDirection: "column",
    gap: "1.25rem",
    paddingTop: "0.5rem",
}));

export default ActionDialogContent;