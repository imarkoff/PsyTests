import styled from "@emotion/styled";
import {Box} from "@mui/material";

const ToolbarOptions = styled(Box)({
    display: "flex",
    alignItems: "center",
    justifyContent: "end",
    flexWrap: "wrap",
    gap: "0.25rem",
});

export default ToolbarOptions;